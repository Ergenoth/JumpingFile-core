#include "testClass.h"

std::string testClass::serverStartup() {
    // Checks if the server started correctly by calling the status api
    // TODO Make a real api call :D
    return "Up and running";
}